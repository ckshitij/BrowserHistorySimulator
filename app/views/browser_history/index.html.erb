<table border="1">
  <tr>
    <th>URLs</th>
    <th>Time</th>
  </tr>
  <% @history.each do |history_entry| %>
    <tr>
      <td><%= history_entry.url %></td>
      <td><%= history_entry.created_at %></td>
    </tr>
  <% end %>
</table>
